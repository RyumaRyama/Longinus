<div class="stats">
  <a href="<%= friends_user_profile_path(account: current_user.account) %>">
    <% friends = current_user.find_friends.size%>
    <div class="text-light">
      フレンド数：
      <strong id="followers" class="stat">
        <%= friends %>
      </strong>
    </div>
  </a>
  <br>
  <a href="<%= follow_requests_user_profile_path(account: current_user.account) %>">
    <% requests = current_user.find_friend_requests.size%>
    <div class="text-light">
      リクエスト中：
      <strong id="followers" class="stat">
        <%= requests %>
      </strong>
    </div>
  </a>
</div>
